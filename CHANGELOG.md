# 变更记录

## 1.2.6

- **转码首字节定了 6 秒预算，超了退回原始音质。** 线上抓到：转码起步慢的
  时候反代等不到响应体就掐断连接，用户拿到 502、一点声音都没有 ——
  而应用日志里记的是 200。这比"降级到原始音质"糟得多。
  首字节现在**在响应头发出去之前**就拿到手，进了流式响应再失败就没机会
  改主意了。预算按实测取（正常 1.7–3.7 秒，见过 13.2 秒）。
- 转码失败时也把会话还给 Plex（原来只有成功的流在结束时还）。


## 1.2.5

- **每次调 Plex 都带上 `X-Plex-Client-Identifier`。** 转码会话是按客户端
  归属的，`stop` 不带这个头，Plex 匹配不上会话直接回 404 —— 名额没还回去，
  下一次 start 就被 400 拒掉。表现是"码率一次成一次败地交替"。
- 排查方式记一笔：Plex 在 NAS 的局域网里，从容器内直接打它的转码接口，
  比每次发版再验快得多。**关键判据是"第一个探测成功、后面参数完全相同的
  重复全部 400"** —— 那一眼就排除了"某些码率不被支持"，指向会话名额。
  （之前隔着应用看，连着两轮得出过完全相反的"稳定结论"。）


## 1.2.4

- **修 1.2.3 自己引入的线上 502。** 1.2.3 把 Plex 的错误正文放进了
  `X-SongLib-Stream-Note` 响应头，而那是带换行的 HTML —— 响应头值里出现
  `\r\n`，ASGI 服务器直接报错把连接掐掉，反代回一个 502。表现是某些码率
  **完全放不出声**，比它想诊断的那个静默降级还糟。截断长度不够，控制字符
  必须先清掉。加了 3 条守卫钉住这件事（其中一条当场抓出 `\x00` 不属于
  `\s`、只压空白是漏的）。
- 排查纪录：那个 502 只用 470ms 就回来了 —— 不是超时，是连接被掐。
  先量再下结论救了一次，本来已经在按"反代读超时"的方向改了。


## 1.2.3

- 转码会话的 stop 同时打 `/music/:/…/stop` 和 `/video/:/…/stop`。文档普遍
  写的是后者，但我们是从 `/music/:/…/start.mp3` 起转的 —— 停错了命名空间
  等于没停，会话照旧泄漏。症状是"同一个码率忽好忽坏、两轮结果正好相反"
  （1.2.2 实测：第一轮 256k 成 128k 败，第二轮反过来）。
- 退回原因里带上 Plex 的响应体。httpx 的异常消息只有状态码和 URL，
  而 Plex 把拒绝的理由写在正文里；不读出来就只剩一个光秃秃的 400。


## 1.2.2

- 转码退回原始音质时，把 Plex 那边的失败原因带在
  `X-SongLib-Stream-Note` 响应头里。退回本身是静默的（声音照出），
  但原因不能跟着消失 —— 线上实测 320k/192k 能转、256k/128k 稳定被拒，
  兜底把错误吞了，查不出为什么。


## 1.2.1

播放（1.2.0 上线后实测发现的）
- **转码会话从来没回收过。** 每换一首就漏一个转码名额，Plex 的并发转码有
  上限，漏满之后新会话一律被拒。线上量出来的症状很怪：同一个码率两轮测
  "能转"的档位正好相反 —— 那不是某个码率坏，是名额被自己占满了。这也是
  当初那个 400 的更深层原因。现在一条流一个独立会话 id，用完调
  `/video/:/transcode/universal/stop` 还回去（失败那条路也还）。
- 转码档能拖进度了。转码流没有字节范围（线上量到 `accept-ranges: none`），
  Plex 的做法是从某个时间点重新起转。现在把浏览器要的字节位置按 CBR 换算
  成秒喂给 `offset`，自己声明 content-range —— 之前音质选项只做了一半。

PWA
- **Chrome/Edge 不弹窗的根因是监听时机**，不是 manifest。
  `beforeinstallprompt` 只发一次、发得很早，而监听器注册在 PwaInstallPrompt
  的 effect 里 —— 那个组件只在登录之后才挂载，等它挂上来事件早过去了。
  现在在 main.jsx 里、React 之前就把事件接住存起来。
- 桌面版 macOS Safari 原来落到兜底分支，被告知"去 Chrome 或 Edge 的地址栏
  找安装音屿" —— 用户明明在 Safari 里。Safari 17 起可以「文件」→「添加到
  程序坞」，现在说的是这句。Firefox 桌面版直接说明装不了，别让人白找菜单。
  iOS 一如既往不承诺"直接安装"（Safari 没有这个 API），只给到位的手动步骤。

海报（先搞清基线语义，一个系数都没动）
- `paintText` 的 y 是**块的顶部**，不是基线。「封面歌词」和「拍立得」把块高
  加进了顶部坐标 —— 这两处是 paintText 早期收基线坐标时写的，函数改了之后
  没跟着改。3:4 实测：曲名基线 1289 而按 textTop 算应该是 1228，整块低一个
  自身高度，曲名和歌手之间设计好的 24px 间距被吃光还叠了 1px。
  「歌词」「极简」两个模板的用法一直是对的。
- **1:1 方版上「专辑」模板的曲名/歌手/专辑整块画在画布外**（1042–1234，
  画布只有 1080 高），方版海报上这三行完全看不见。封面尺寸原来定死成
  `width - pad*2`，不看画布高。改成吃剩余高度；3:4 / 9:16 算出来还是满宽，
  分毫未动。
- 1:1 上「拍立得」的相纸本身就比画布高 54px，手写区被挤出去。相纸宽度改成
  受画布高度约束。
- 1:1 四句歌词时最后一行压在曲名上。字号已经压到下限，放不下就少画一行。
  顺带修掉块高只算到基线、没算下伸的老账（`layoutText` 里早就修过一次，
  手写的块高数学没享受到）。

守卫
- 海报 +54 条：每个模板 × 比例 × 句数，逐个字的盒子摆出来看有没有撞上、
  有没有出画布。上面这些问题，NaN 检查和只数空白带的留白检查全都放行了。
- `interact-check.mjs` +1 段：在 React 之前派发一个假的 beforeinstallprompt，
  验证捕获仓接住了、preventDefault 了、按钮变成「安装应用」、点了真调 prompt()。
- 后端 +5 条：会话回收（成功与失败两条路）、seek 换算成时间 offset、
  Range 不许转发给上游、每次请求独立会话 id。


## 1.2.0

播放（唯一让应用不能用的那个）
- Plex 转码接口对 `protocol=hls&maxAudioBitrate=320` 回 400，我们原样包成 502
  抛给 <audio>，播放器直接死。转码地址换成 Plex Web 自己在用的那套：
  `start.mp3` + `protocol=http` + `musicBitrate`，并补上 `session` 和
  `X-Plex-Client-Identifier`（HLS 那条路要求带这两个，缺了就被拒；而且 HLS
  只有 Safari 的 <audio> 认，Chrome 拿到播放列表也放不出声）。
- **不管转码为什么失败，一律自动退回原始音质**，不再抛 502。原始音质是直读
  文件，实测稳定（206，首字节 1.26s）。响应头带 `X-SongLib-Stream-Mode`，
  用户能看出"点了 320K、实际给的是原始音质"。
- 音质设置从两处合并成一处。「用户偏好 → 默认音质」（选项里还有 FLAC / Hi-Res，
  那是下载音质）压过了「播放器设置 → 远程默认 320K」，用户取消了后者、前者
  还在发 320k。现在远程播放音质只在播放器设置里，做成真正的五档下拉；
  本机在播放器上选过就以本机为准；那个下载音质正名为「下载音质」。
- 恢复上次那首时不再写死原始音质 —— 之前每次刷新都把用户选的档位悄悄降回去。

移动端
- 迷你播放条压住底栏导航。胜出规则是 legacy.protected 层的
  `.mini-player{bottom:78px}`，它无视特异性压掉了带 env(safe-area-inset-bottom)
  的版本。真 iPhone 上安全区 34px、底栏 98px 高，迷你条底边还固定在 78px，
  正好压住 20px。无头浏览器安全区恒为 0，所以本地怎么量都是"没问题"。
  现在底栏和迷你条各自把量到的真实高度写成 CSS 变量，偏移只做加法，
  字号、安全区、底栏格数变化全自动跟上。
- 迷你条能收起了。之前想让它别挡路只能把歌关掉（唯一的 X 是"停止播放并清空
  队列"）。收起后缩成右下角一个胶囊，歌继续放，状态记在本机。
- 首页 hero 封面窄屏下歪向一边：封面 218px 在 316px 的列里贴左，左 21 右 119。
  根因是 `justify-items: start` —— 它连带把文字列也收缩到 276px，标题白白
  提前折行。改成 stretch + 封面自己居中，衬底遮罩也从"向右渐隐"改成竖排的
  "向下渐隐"。

Plexamp
- 只显示「仅跟随」、点了没反应：`controllable` 要求设备出现在服务端
  `/clients`，而 **Plexamp 不注册到 /clients**（那是老式 Plex 播放器才做的
  事），它只向 plex.tv 报到。现在同时读 plex.tv 的设备清单，取局域网内的
  私网连接地址 —— 只往私网地址发控制请求这条约束不变。
- 卡顿：这个接口前端每 4 秒轮一次，而它走的是 60 秒默认超时。Plexamp 在线时
  `/clients` 一慢，单次轮询能阻塞 60 秒，前端又会把后续轮询全部合并掉，
  界面时钟先冻住再跳一大步。改成 3 秒预算，慢就当这一轮没拿到。

守卫
- 后端 +7 条：转码失败退回原始音质、转码成功不许降级、两条都挂才报 502、
  转码地址必须带 session 与客户端标识、Companion 发现、只有私网地址可控、
  plex.tv 够不到时 /clients 那条路仍可用。
- `interact-check.mjs` +2 段：迷你条几何（含注入 34px 安全区再验一遍）与
  收起/展开/持久化；首页 hero 封面居中与文字列宽度。


## 1.1.9

- 投屏编码器跑在播放前面。歌词画面完全由时间轴决定，后面几十秒长什么样
  现在就知道，所以能先编出来，不必卡着实时喂帧。公网提前编 45 秒，
  编够之前全速、编够之后回到实时节奏。配合 1.1.8 的"不删分片"，
  接收端才囤得下一大段，网络抖一下不会断。
- 局域网不提前编：那边要的是低延迟，提前编只会让 seek 之后白扔更多画面。

## 1.1.8

投屏
- 公网不再删分片。之前以为外网卡死是分片太短，把 1 秒调到 4 秒 —— 那只是
  推迟。真正的元凶是 delete_segments：客户端外网抖动一次就落后，它要的
  分片已被删掉，之后再也追不回来。现在列出全部分片，落后多少都能追。

首页
- 首屏大卡改成轮播，8 张最近加入的可以左右切，7 秒自动换一张，手动切过
  之后就不再自动跑。
- 右侧原来空一大块，现在放这张专辑的曲目，点任意一首从那首开始放。
- 「听到一半」并进「继续播放」。两块内容高度重叠，同一首歌在两处各出现
  一次、一处带进度一处不带。现在就一块，听了一半的那几首多一条进度条，
  点它直接续播。

设置页
- 一竖排图标左边界在 62/68/72/74 之间游走。按钮宽度和左边界其实完全一致，
  歪的是里面的内容：更广的规则把【图标+文字】当整体居中，文字长度不同
  图标就被推到不同位置。

移动端
- 手机上打不开全屏歌词：不是被遮住，是按钮落在 y=864 而视口只有 844，
  在屏幕外面，主区又被定死高度滚不过去。窄屏取消定高，回到竖排长页面。
- 歌词区双击也能开全屏。

## 1.1.7

- 页面切换走 View Transitions。原来只有新页面淡入，旧页面是瞬间消失的。
- 首页大封面随滚动下沉。时间轴必须用 scroll(root)，nearest 在没有
  overflow 祖先时静默不激活；关键帧只动 translate，动 transform 会把
  元素原有的 scale 顶掉。
- 极简与歌词两个海报模板的纵向重心。之前两处改动因为替换锚点没对上、
  静默没写进文件，所以一直是老的纯居中：内容从 36-39% 处才开始，
  全挤在下半部。极简改成上三分之一重心，不管画布多高比例都恒定。
- 海报构图检查换了判据并重新定阈值。整行 max-min 会被对角渐变骗到
  （纯背景被判成内容），改用相邻像素跳变；阈值按实测三类行的数值定在
  7（空白 1-3、唱片盘身 11-16、文字 58-228）。底部水印是页脚不是内容，
  不再参与"画面裂开"的计算。

## 1.1.6

- 修输入框重影边框：外壳 .ui-field__control 和里面的 input 各画了一层
  同色同圆角的边框，Plex 配置弹窗里每个字段都套着一个空框。成因是
  settings.css 那条通用 input 规则（特异性 0,5,1，一个类加四个属性型
  :not()）伸进了弹窗 —— 弹窗虽渲染在 top layer，DOM 上仍是 .settings-page
  的后代。把通用规则收窄到"无类名 label 的直接子控件"。
- 交互脚本新增通用重影边框检测：扫全站找"父子同色同宽边框且尺寸几乎
  一样"的组合。这类缺陷成因相同，人眼在小截图上容易忽略。

## 1.1.5

投屏
- 参数按网络分档。原来一律是局域网的 1 秒分片、12 秒窗口；公网上客户端
  一落后 12 秒，要的分片已被删掉，再也追不回来 —— 症状就是"显示一屏
  歌词然后不动"。公网改 4 秒分片、48 秒窗口、帧率 30 降 15。
- 主播放列表原来写死 FRAME-RATE=30，公网档实际编 15，声明和流对不上。

正在播放页
- 主区定高漏算了页面 120px 的下内边距，整页比视口高出 78px：定高了、
  页面照样能滚。改用 --now-chrome: 228px，实测归零。
- 歌词模板的余量摊进行距，不再堆到某一侧。之前三轮都在"把空白挪个位置"：
  纯居中头重、靠上锚定中间裂开、掺一半余量两个毛病各占一半。
- 全屏歌词遮罩改成不透明（原来用的是给"压在图片上一层"的 scrim token，
  底下整页透出来）；退出按钮给实底和阴影，之前压在虚化封面上看不出是按钮。

海报
- 黑胶方版专辑名压在底注上。现在先给底注留位，放不下就不画专辑行。

动效
- 指针跟随光晕：写 CSS 变量、不走 state，rAF 节流，只在有精确指针的设备
  上挂。用回调 ref 而不是空依赖的 effect —— 首页 hero 是条件渲染，
  挂载时还不存在，空依赖只会跑一次拿到 null。
- 滚动手感：歌词面板和弹窗加 overscroll-behavior: contain（原来滚到底会
  把整页带走）、长列表 content-visibility、按钮 touch-action: manipulation。
- prefers-reduced-motion 全站兜底。

## 1.1.4

- 修复 1.1.3 引入的回归：首页那个"最近加入按天轮换"的 useMemo 写在了
  `if (loading) return` **之后**，加载中那次渲染少一个 hook、加载完多一个，
  React 抛 #310 整棵树崩掉，页面永远停在"正在连接本地音乐库"。
  mock 同步返回数据、走不到 loading 分支，所以本地一次都没崩。
  这个 bug 是把交互脚本跑在真部署上才发现的。
- interact-check.mjs 支持 SL_STATE 复用已登录会话，可以直接跑真站。

## 1.1.3

交互与布局
- 分享图选了别的样式一秒后被按回默认（默认值 effect 每秒重跑）。
- 新增「封面歌词」「拍立得」两个模板，样式从 4 个变 6 个。
- 歌词模板 9:16 顶部起点由 height*0.2 改成按宽度算，上下配平。
- 歌词面板原先没有高度约束，有多少行就长多高，滚的是整个页面；
  现在主区受视口约束，歌词在面板内部滚。
- 歌词跟着歌走（当前句居正中，手动滚后让开 2.6 秒）。全站原先没有
  一处 scrollIntoView。
- 全屏歌词开着时锁住底层滚动，Modal 同类问题一并修。
- 歌词字号 30px 降到 20px；版权行（词/曲/编曲/OP/SP、歌名-歌手抬头）
  降成小字，原先和歌词同一个字号顶在最上面占掉半屏。
- 移动端底栏只显示「首页」一格：utilities 层的 display:revert 把
  .mobile-nav 的 grid 抹成了 <nav> 默认的 block。
- 移动端登录页表单跑到品牌区上面（order:-1），改回自然顺序。
- 设置页用户偏好那列字段挤在右侧、宽度各不相同：legacy grid 的
  align-items:end 在换成 flex column 后没被清掉。
- 设置页权限勾选框被拉成五个大方块（同一类特异性问题）。
- 首页大封面在最近加入的前 8 张里按天轮换，不再永远同一张。

投屏
- 投屏地址不再写死。它是给 Apple TV 用的，必须是 Apple TV 够得着的
  地址，所以跟着"浏览器用哪个地址进来"走：在家是内网 IP，在外网是
  https 域名。原先写死家里的内网 IP，人在外网时必然失败。

工程
- 新增 scripts/interact-check.mjs：真的去点、去等、去滚。之前验收只
  渲染首屏截图，而这一批 bug 全是"上手 30 秒才遇到"的类型。
- mock 的音频流原先返回 204 零字节，currentTime 恒为 0，所有依赖播放
  推进的行为在本地都验证不了。改成发一段真的静音 WAV（支持 Range）。
- mock 歌词换成含版权块的长歌词，滚动和版权降级这才测得到。

## 1.1.2

- 修复应用跑在 HTTPS 反代后面时，把自己的静态资源拦成 403 导致整站白屏。
  同源判定原先用 uvicorn 看到的 scheme 拼预期来源，反代卸载 TLS 之后那
  是内网 http，而浏览器发的 Origin 是 https，对不上就拦。现在 host 仍要
  完全一致，scheme 额外接受 https。
- 密码最短长度统一成 10 位。原先初始化向导要 12 位、后台建用户和改密码
  要 10 位，第一次设密码反而更严。

## 1.1.1

- 清掉最后四处第二人称文案：登录页、首页、我的页、初始化向导。
  这几处是 JSX 文本不是字符串，上一轮按引号扫的时候漏了。

## 1.1.0

设计系统与文案重做，外加几处真 bug。

界面
- 全站统一一套设计 token 与基础组件（按钮、卡片、字段、路径、变更清单），
  按钮不再有四种同优先级外观，对齐做进组件本身。
- 浅色主题补齐：顶栏、搜索框、正在播放页原先有硬编码深色，浅色下是
  整屏里几块黑。现在 13 个 `--now-*` 变量全部映射到语义 token。
- 正在播放页：封面、曲名、控制条共用一条随视口高度收缩的列宽；控制条
  第二行改成音量靠左、喜欢与分享靠右；进度条和音量条画出已播部分。
- 圆形歌手封面的播放按钮不再被裁一半 —— 按钮从封面里挪出来，圆形封面
  按 45 度角定位。
- 长路径省略开头而不是结尾，并用 LRM 修掉双向文本把首尾斜杠搬到另一端
  的老毛病。
- 所有页面文案改成陈述句：标题和字段名用名词，空状态一句话，按钮用动词。

新功能
- 分享海报：四个模板、三种比例，封面取色，歌词海报与歌曲海报。
- 跨设备续播：换设备时问一句"上次听到 3:20，接着听？"，从不自动跳。
  本机播放和跟随 Plexamp 都会记位置。
- 曲库体检：重复、缺封面、缺歌词、目录不规范、文件已不在，一次列清。
- 操作历史与批量回滚。
- 离线索引：断连时能翻已经浏览过的内容。

修复
- AirPlay 投屏画面的封面遮罩层原先直接覆盖像素、连 alpha 一起写掉，
  投到 Apple TV 上的那一层压暗和描边是不透明的实色块。改成独立图层
  合成。启动时会检查系统里有没有能渲染中文的字体，没有就直接拒绝启动，
  而不是投出一屏豆腐块。
- 判重的时长分桶有硬边界：215 秒和 216 秒只差一秒却分在两个桶里，
  这一对重复查不出来。改成按 ±3 秒容差聚类，体检和入库前查重共用
  同一个常量。
- 搜索失败被当成"没有结果"：三路查询各自 catch 成空数组，NAS 连不上
  时看起来就是"搜到 0 条"，离线兜底永远不会触发。
- 音源管理的"试搜一首"没有输入框，点下去只提示要填关键词。
- 设置页一条无类名的 label 规则漏进了 Plex 对话框，把里面的网格布局
  压成了纵向堆叠。

## 1.0.5

- 修复已登录且存在当前播放曲目时，侧边栏/底部迷你播放器调用缺失的 `formatTime` 导致 React 首屏直接崩溃、页面永久停留在“正在连接本地音乐库”的问题。
- 增加登录后播放器壳层的回归检查，并升级启动恢复脚本与 PWA 静态缓存版本。

## 1.0.4

- 将播放器高频时钟拆出全局应用上下文，播放进度更新不再触发整个页面树重绘；iPad 登录页移除运行时动画依赖、大型背景视频和持续粒子动画。
- AirPlay 投屏改为“曲目/歌词元数据按变化发送 + 每秒轻量时钟心跳”，避免重复上传大段歌词；Plex 封面暂时不可用时不再阻断标题、歌词与进度更新。
- 修复 0.5 秒 HLS 被 FFmpeg 生成非法 `EXT-X-TARGETDURATION:0` 导致 Apple TV 转圈和断流的问题，默认使用合规的 1 秒 fMP4 分片；连续切歌保持同一 URL 和同一编码器。
- 修复移动端底部导航同时高亮“播放”“工具”“设置”的视觉状态错误；登录、首页、播放、工具、设置和文件标签页完成 iPad Pro 10.5 英寸视口验收。
- 本地文件读取、播放、封面与歌词接口统一执行当前用户的曲库权限范围检查。

## 1.0.3

- 增加不依赖 JavaScript 的轻量启动画面，低性能 iPad 在前端初始化期间不再只看到纯黑背景。
- 触控设备登录页取消初始透明和位移动画，表单挂载后立即可见、可点击、可输入。

## 1.0.2

- 修复 iPad/iPhone 登录页继承桌面端全屏垂直居中后，表单落到首屏下方而看起来只有黑屏的问题。
- 移动端登录页改为从安全区顶部自然排布，并压缩不必要的垂直留白，保证账号、密码和登录按钮可立即触控。
- 更新 PWA 静态缓存版本，避免主屏幕应用继续使用存在黑屏布局问题的旧样式。

## 1.0.1

- 修复 Safari 原生 AirPlay 选择器退化为音频封面页：歌词 HLS 在用户点击时先开始播放，并在视口内显示真实视频预览，再交给 `webkitShowPlaybackTargetPicker()`。
- 禁止 SongLib 本机音频元素参与远程播放，避免它抢占歌词视频的 AirPlay 路由。
- PWA Service Worker 更新后自动接管并刷新页面，避免长期驻留的 iPad 主屏幕应用继续运行旧前端。
- NAS 部署要求显式设置 Apple TV 可达的 `AIRPLAY_PUBLIC_BASE_URL`，不再依赖管理入口或反向代理传入的 Host。

## 1.0.0

- 修复外部 Plexamp `viewOffset` 冻结造成的进度条周期回跳，改为单调预测、小漂移限幅修正和真实 seek 重锚；远端歌词改为优先读取 Plex 自带 LRC 字幕流。
- Apple TV 歌词画面恢复早期电视原型的左封面/右五行歌词布局；HLS 增加静音 AAC、闭合 GOP、0.5 秒 fMP4 分片和直播延迟补偿，避免 Safari 退化到只显示封面的音频 AirPlay 页面。
- 新增 `±0.25s` 会话微调、标准媒体播放/暂停桥接和 Media Session 切歌渐进增强；明确 HLS 不能伪装成可浏览歌单的原生 tvOS 界面。
- 修复 iPad 主屏幕模式登录表单无法触控/唤起键盘，并正确说明内网 HTTP 页面仍可添加到主屏幕和在线使用；Service Worker、离线缓存与推送才需要安全上下文，不再要求内网流量绕行公网。
- iPad Pro 10.5 英寸竖屏改用触控平板布局和六项底部主导航，关闭大面积实时模糊、登录视频与持续动画，避免侧栏挤压和合成层卡顿。
- 新增 Safari 原生 AirPlay 歌词投屏原型：系统设备选择器、固定会话 fMP4 HLS、切歌不中断、普通/增强 LRC 高亮、双时钟漂移修正、QSV/4K 配置与软件编码降级。
- 下载到当前设备改为浏览器原生下载通道，增加逐项忙碌状态、错误反馈与操作留白；NAS 下载按音乐源声明自动尝试兼容的较低音质。
- 补齐 LX 音乐源常见的 `types`、专辑、图片等曲目字段，并按平台设置下载请求头，提升第三方授权音乐源兼容性。
- 播放器升级为独立全屏页面：单张封面模糊背景、左上返回、右下播放队列抽屉、双栏封面与同步歌词布局，并完成移动端正方形封面约束。
- 新增深色、浅色和跟随系统三种主题；设置中心可实时调节玻璃模糊、透明度、背景模糊与可见度、字号、圆角、饱和度和动效强度。
- 全站卡片统一为可调液态玻璃层级；浅色主题采用干净的高对比留白与半透明层级。
- 仪表盘、设置、任务和音乐源使用会话级快速缓存即时恢复，任务轮询降频并在页面隐藏时暂停；前端依赖拆分为可长期缓存的独立包。
- Service Worker 升级为资源优先缓存和导航预加载，认证 API 与音频流不进入离线缓存。

## 1.0.0-rc.5

- 歌手与专辑详情只接受归属于当前 Plex 歌手 `ratingKey` 的背景路径，拒绝继承自其他歌手的错误 `art`。
- 全局背景改为单图层替换，不再让前后两张大图长时间叠加；歌手与专辑详情都取消内容区背景大卡片。
- 背景轮播只使用按曲目数排序的 Plex 歌手背景，最多 80 张，不再混入本地目录背景。
- 音乐源识别到接口后自动开放搜索与下载，测试失败只记录运行状态，不再撤销使用权限；下载任务取消同步预检门槛。
- PWA 安装入口只在浏览器提供原生安装事件时直接安装；普通 HTTP 或不满足条件时展示明确可执行的安装说明，不再出现无响应按钮。
- 移动端黑胶唱片使用固定等宽尺寸，统一详情、来源、结果与主操作按钮的高度、图标和对齐方式。
- 曲库首批数据立即显示、剩余分页并行补齐；Plex 仪表盘统计增加短时缓存，降低重复全库查询和全屏模糊开销。
- 版本升级为 `1.0.0-rc.5`，同时发布 `latest`、`linux/amd64` 与 `linux/arm64` 镜像。

## 1.0.0-rc.4

- 音乐库可读取全部歌手与专辑，并增加稳定的歌手与专辑详情地址。
- 修复设置导航同时高亮管理中心的问题，增加用户字号偏好。
- 飞牛音乐支持账号密码换取服务会话；密码不保存，令牌写入受保护数据目录。
- 音乐源通过格式与安全校验后默认启用，在线测试改为健康状态。
- “下载与入库”实时读取已导入音乐源，不再要求先完成搜索测试；首次下载自动执行解析与音频探测。
- 歌单页汇总已连接的 Plex 与飞牛音乐真实歌单，并保留 M3U、平台分享链接和双向同步入口。
- Plex 服务歌单兼容毫秒时长，整单加入播放器队列并按原顺序自动续播；从歌单中间播放时保留后续曲目。
- 歌词读取支持大小写不同的 LRC/TXT、UTF-8/UTF-16/GB18030/Big5 和音频内嵌标签，随附歌词缺失时再执行严格匹配。
- 全局背景按 Plex 歌手曲目数排序，从最多 80 张歌手背景中随机轮播；统一高透玻璃卡片层级。
- 首页主视觉改为歌手圆形封面嵌入黑胶中心的低速微动唱片；歌手详情移除独立背景大卡片，并在整个详情期间锁定当前歌手的 Plex 背景。
- 歌手介绍支持展开，页面宽度、侧栏 Logo 与版本位置统一。
- 移动端 PWA 固定为单行五项主导航，迷你播放器不再挤出第二行。
- 默认 Compose 收敛为单个 `songlib` 服务、三个核心目录挂载，并从 Docker Hub 拉取唯一维护的 `latest`。
- Compose 支持按 NAS 账号的 PUID/PGID 以非 root 身份访问绑定目录；发布 amd64/arm64 多架构镜像。

## 1.0.0-rc.3

- 重建推荐、我的音乐与管理中心的信息层级，移除无意义趋势线和重复卡片。
- 恢复低干扰的轮播媒体背景，并统一图标、按钮、行高和留白。
- 为曲库标签、歌单详情与管理功能加入可复制、可前进后退的二级地址。
- 修复推荐页因未定义通知函数导致的整页崩溃，并补充响应数据容错。
- 明确飞牛音乐密码登录与 NAS 授权边界，避免读取或复用浏览器会话。

## 1.0.0-rc.2

- 重新设计应用信息架构与音乐首页，管理状态不再占据首页主视觉。
- 统一侧栏、工具栏、按钮、图标、媒体网格、曲目表格与响应式规格。
- 重构正在播放、队列、迷你播放器和全屏歌词布局。
- 增加 QQ 音乐、网易云音乐公开歌单链接预览与迁移，可同步音屿、Plex 和已配置的飞牛音乐。
- 歌单缺失歌曲采用标题、主要艺人和时长严格校验后进入授权下载队列。
- 下载挂载与正式音乐库彻底分离，增加下载目录扫描、Unicode/疑似乱码规范化、冲突预览、整理入库和回滚。

## 1.0.0-rc.1

- 新增无默认弱密码的首次安装向导。
- 新增 CSRF、Origin/Host 校验、登录限速、安全响应头和脱敏审计。
- 将后台任务升级为可续租、可重试、可恢复的持久队列，并拆分独立 Worker。
- 新增版本化数据库迁移、媒体/提供方连接、路径映射和隔离区模型。
- 新增多用户歌单、M3U/M3U8 导入导出、顺序保持和未匹配报告。
- 新增本地播放事件、可解释画像、探索比例、去重与版本过滤推荐。
- 新增原创墨蓝、海沫绿与琥珀视觉层，补齐首装、空状态、错误状态和移动端布局。
- Compose 改为独立桥接网络、非 root、只读根文件系统和 Web/Worker 双服务。
- 新增 amd64/arm64 构建、健康探针、备份恢复脚本、部署/升级/安全文档和 CI。

## 0.8.0

- 初始公开基线。
