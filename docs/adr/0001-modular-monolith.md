# ADR 0001：模块化单体与独立 Worker

状态：已接受

NAS 用户更需要可预测的部署和备份，而不是多个数据库与消息中间件。因此 Web 与领域代码保留在同一镜像，后台任务以独立 Worker 进程运行。两者通过 SQLite 持久队列协作。

结果：部署只需要 Docker Compose 和一个数据卷；Web 崩溃不会终止 Worker。未来负载增长时，适配器和任务接口可以迁移到独立服务。
